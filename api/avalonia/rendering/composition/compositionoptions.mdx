---
title: 'CompositionOptions'
hide_title: true
slug: '/avalonia/rendering/composition/compositionoptions'
uid: 'Avalonia.Rendering.Composition.CompositionOptions'
commentId: 'T:Avalonia.Rendering.Composition.CompositionOptions'
docset: 'avalonia-docs'
packageId: 'Avalonia'
assembly: 'Avalonia.Base'
apiKind: 'Class'
generatedBy: 'dotnet-apiref'
namespace: 'Avalonia.Rendering.Composition'
tfms:
  - 'net10.0'
---

import ApiRefPage from '@site/src/components/apiref/ApiRefPage';

<ApiRefPage>

<h1 className="apiref-page__title">CompositionOptions Class</h1>

## Definition

<div className="apiref-page__definition-meta">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Namespace:</span> <a href="./">Avalonia.Rendering.Composition</a>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Assembly:</span> <code>Avalonia.Base</code>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Package:</span> <code>Avalonia</code>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Source:</span> <a href="https://github.com/AvaloniaUI/Avalonia/blob/649891b3481c6e1581f42efc8f5f106f86ef6119/src/Avalonia.Base/Rendering/Composition/CompositionOptions.cs">CompositionOptions.cs</a>
</div>
</div>

<div className="apiref-page__signature">

```csharp
public class CompositionOptions
```

</div>

<div className="apiref-page__relations">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Inheritance:</span> object <span className="apiref-page__relation-separator">-&gt;</span> CompositionOptions
</div>
</div>

## Constructors

| Name | Description |
| --- | --- |
| [CompositionOptions](#uid-592365acc8) | No summary available. |

<section className="apiref-page__member-entry">

<span id="uid-592365acc8"></span>

### CompositionOptions Constructor

```csharp
public CompositionOptions()
```

</section>

## Properties

| Name | Description |
| --- | --- |
| [DirtyRectMergeEagerness](#uid-285c3acc1c) | Controls the eagerness of merging dirty rects. WPF uses 50000, Avalonia currently has a different default that's a subject to change. You can play with this property to find the best value for your application. |
| [MaxDirtyRects](#uid-210bada917) | The maximum number of dirty rects to track when region clip is in use. Setting this to zero or negative value will remove the smarter algorithm and will use underlying drawing context region support directly. Default value is 8. |
| [UseRegionDirtyRectClipping](#uid-f64a343f80) | Enables more accurate tracking of dirty rects by utilizing regions if supported by the underlying drawing context |
| [UseSaveLayerRootClip](#uid-464a94f787) | Enforces dirty contents to be rendered into an extra intermediate surface before being applied onto the saved frame. Required as a workaround for Skia bug https://issues.skia.org/issues/327877721 |

<section className="apiref-page__member-entry">

<span id="uid-285c3acc1c"></span>

### DirtyRectMergeEagerness Property

Controls the eagerness of merging dirty rects. WPF uses 50000, Avalonia currently has a different default
that's a subject to change. You can play with this property to find the best value for your application.

```csharp
public Nullable<double> DirtyRectMergeEagerness { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-210bada917"></span>

### MaxDirtyRects Property

The maximum number of dirty rects to track when region clip is in use. Setting this to zero or negative
value will remove the smarter algorithm and will use underlying drawing context region support directly.
Default value is 8.

```csharp
public Nullable<int> MaxDirtyRects { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-f64a343f80"></span>

### UseRegionDirtyRectClipping Property

Enables more accurate tracking of dirty rects by utilizing regions if supported by the underlying
drawing context

```csharp
public Nullable<bool> UseRegionDirtyRectClipping { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-464a94f787"></span>

### UseSaveLayerRootClip Property

Enforces dirty contents to be rendered into an extra intermediate surface before being applied onto the
saved frame.
Required as a workaround for Skia bug https://issues.skia.org/issues/327877721

```csharp
public Nullable<bool> UseSaveLayerRootClip { get; set; }
```

</section>

</ApiRefPage>

