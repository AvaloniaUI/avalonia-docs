---
title: 'Grid'
hide_title: true
slug: '/avalonia/controls/grid'
uid: 'Avalonia.Controls.Grid'
commentId: 'T:Avalonia.Controls.Grid'
docset: 'avalonia-docs'
packageId: 'Avalonia'
assembly: 'Avalonia.Controls'
apiKind: 'Class'
generatedBy: 'dotnet-apiref'
namespace: 'Avalonia.Controls'
tfms:
  - 'net10.0'
---

import ApiRefPage from '@site/src/components/apiref/ApiRefPage';

<ApiRefPage>

<h1 className="apiref-page__title">Grid Class</h1>

## Definition

<div className="apiref-page__definition-meta">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Namespace:</span> <a href="./">Avalonia.Controls</a>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Assembly:</span> <code>Avalonia.Controls</code>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Package:</span> <code>Avalonia</code>
</div>
</div>

<div className="apiref-page__lead">

Defines a flexible grid area that consists of columns and rows.

</div>

<div className="apiref-page__docs-links">

:::info Documentation
For conceptual guidance and examples, see:
- [Grid panel](/controls/layout/panels/grid)
- [How to use Grid](/docs/how-to/grid-how-to)
:::

</div>


<div className="apiref-page__signature">

```csharp
public class Grid
```

</div>

<div className="apiref-page__relations">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Inheritance:</span> Object <span className="apiref-page__relation-separator">-&gt;</span> <a href="../avaloniaobject">AvaloniaObject</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../animation/animatable">Animatable</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../styledelement">StyledElement</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../visual">Visual</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../layout/layoutable">Layoutable</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../interactivity/interactive">Interactive</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="../input/inputelement">InputElement</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="control">Control</a> <span className="apiref-page__relation-separator">-&gt;</span> <a href="panel">Panel</a> <span className="apiref-page__relation-separator">-&gt;</span> Grid
</div>
</div>

## Constructors

| Name | Description |
| --- | --- |
| [Grid](#uid-9d1f640e2f) | Default constructor. |

<span id="uid-9d1f640e2f"></span>

### Grid

Default constructor.

```csharp
public Grid()
```

## Methods

| Name | Description |
| --- | --- |
| [GetColumn](#uid-a3f1fbdcf9) | Helper for reading Column property from a Control. |
| [GetColumnSpan](#uid-6c780d1497) | Helper for reading ColumnSpan property from a Control. |
| [GetIsSharedSizeScope](#uid-67155d8027) | Helper for reading IsSharedSizeScope property from a Control. |
| [GetRow](#uid-2ec01d9f08) | Helper for reading Row property from a Control. |
| [GetRowSpan](#uid-2015eeadd8) | Helper for reading RowSpan property from a Control. |
| [SetColumn](#uid-c46d3adb18) | Helper for setting Column property on a Control. |
| [SetColumnSpan](#uid-c9a9bf1f6c) | Helper for setting ColumnSpan property on a Control. |
| [SetIsSharedSizeScope](#uid-2f5cf090c2) | Helper for setting IsSharedSizeScope property on a Control. |
| [SetRow](#uid-20e22a584c) | Helper for setting Row property on a Control. |
| [SetRowSpan](#uid-dfcdcb4d85) | Helper for setting RowSpan property on a Control. |

<span id="uid-a3f1fbdcf9"></span>

### GetColumn

Helper for reading Column property from a Control.

```csharp
public System.Int32 GetColumn(Avalonia.Controls.Control element)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to read Column property from. |

Returns: Column property value.

<span id="uid-6c780d1497"></span>

### GetColumnSpan

Helper for reading ColumnSpan property from a Control.

```csharp
public System.Int32 GetColumnSpan(Avalonia.Controls.Control element)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to read ColumnSpan property from. |

Returns: ColumnSpan property value.

<span id="uid-67155d8027"></span>

### GetIsSharedSizeScope

Helper for reading IsSharedSizeScope property from a Control.

```csharp
public System.Boolean GetIsSharedSizeScope(Avalonia.Controls.Control element)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to read IsSharedSizeScope property from. |

Returns: IsSharedSizeScope property value.

<span id="uid-2ec01d9f08"></span>

### GetRow

Helper for reading Row property from a Control.

```csharp
public System.Int32 GetRow(Avalonia.Controls.Control element)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to read Row property from. |

Returns: Row property value.

<span id="uid-2015eeadd8"></span>

### GetRowSpan

Helper for reading RowSpan property from a Control.

```csharp
public System.Int32 GetRowSpan(Avalonia.Controls.Control element)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to read RowSpan property from. |

Returns: RowSpan property value.

<span id="uid-c46d3adb18"></span>

### SetColumn

Helper for setting Column property on a Control.

```csharp
public System.Void SetColumn(Avalonia.Controls.Control element, System.Int32 value)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to set Column property on. |
| `value` | Column property value. |

<span id="uid-c9a9bf1f6c"></span>

### SetColumnSpan

Helper for setting ColumnSpan property on a Control.

```csharp
public System.Void SetColumnSpan(Avalonia.Controls.Control element, System.Int32 value)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to set ColumnSpan property on. |
| `value` | ColumnSpan property value. |

<span id="uid-2f5cf090c2"></span>

### SetIsSharedSizeScope

Helper for setting IsSharedSizeScope property on a Control.

```csharp
public System.Void SetIsSharedSizeScope(Avalonia.Controls.Control element, System.Boolean value)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to set IsSharedSizeScope property on. |
| `value` | IsSharedSizeScope property value. |

<span id="uid-20e22a584c"></span>

### SetRow

Helper for setting Row property on a Control.

```csharp
public System.Void SetRow(Avalonia.Controls.Control element, System.Int32 value)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to set Row property on. |
| `value` | Row property value. |

<span id="uid-dfcdcb4d85"></span>

### SetRowSpan

Helper for setting RowSpan property on a Control.

```csharp
public System.Void SetRowSpan(Avalonia.Controls.Control element, System.Int32 value)
```

| Parameter | Description |
| --- | --- |
| `element` | Control to set RowSpan property on. |
| `value` | RowSpan property value. |

## Properties

| Name | Description |
| --- | --- |
| [ColumnDefinitions](#uid-22974d3679) | Returns a ColumnDefinitions of column definitions. |
| [ColumnSpacing](#uid-f8d96a650c) | Gets or sets the size of the spacing to place between grid columns. |
| [RowDefinitions](#uid-bd42f0541a) | Returns a RowDefinitions of row definitions. |
| [RowSpacing](#uid-a5c0a8d988) | Gets or sets the size of the spacing to place between grid rows. |
| [ShowGridLines](#uid-498fec56bd) | ShowGridLines property. |

<span id="uid-22974d3679"></span>

### ColumnDefinitions

Returns a ColumnDefinitions of column definitions.

```csharp
public Avalonia.Controls.ColumnDefinitions ColumnDefinitions { get; set; }
```

<span id="uid-f8d96a650c"></span>

### ColumnSpacing

Gets or sets the size of the spacing to place between grid columns.

```csharp
public System.Double ColumnSpacing { get; set; }
```

<span id="uid-bd42f0541a"></span>

### RowDefinitions

Returns a RowDefinitions of row definitions.

```csharp
public Avalonia.Controls.RowDefinitions RowDefinitions { get; set; }
```

<span id="uid-a5c0a8d988"></span>

### RowSpacing

Gets or sets the size of the spacing to place between grid rows.

```csharp
public System.Double RowSpacing { get; set; }
```

<span id="uid-498fec56bd"></span>

### ShowGridLines

ShowGridLines property.

```csharp
public System.Boolean ShowGridLines { get; set; }
```

## Fields

| Name | Description |
| --- | --- |
| [ColumnProperty](#uid-fb7a0fa0f1) | Column property. This is an attached property. Grid defines Column property, so that it can be set on any element treated as a cell. Column property specifies child's position with respect to columns. |
| [ColumnSpacingProperty](#uid-61e77f55e1) | Defines the Avalonia.Controls.Grid.ColumnSpacing property. |
| [ColumnSpanProperty](#uid-1f56a4de32) | ColumnSpan property. This is an attached property. Grid defines ColumnSpan, so that it can be set on any element treated as a cell. ColumnSpan property specifies child's width with respect to columns. Example, ColumnSpan == 2 means that child will span across two columns. |
| [IsSharedSizeScopeProperty](#uid-f747297da8) | IsSharedSizeScope property marks scoping element for shared size. |
| [RowProperty](#uid-439d1f6eff) | Row property. This is an attached property. Grid defines Row, so that it can be set on any element treated as a cell. Row property specifies child's position with respect to rows. |
| [RowSpacingProperty](#uid-ccd184b399) | Defines the Avalonia.Controls.Grid.RowSpacing property. |
| [RowSpanProperty](#uid-ec23e31fee) | RowSpan property. This is an attached property. Grid defines RowSpan, so that it can be set on any element treated as a cell. RowSpan property specifies child's height with respect to row grid lines. Example, RowSpan == 3 means that child will span across three rows. |
| [ShowGridLinesProperty](#uid-17fdb6b70c) | ShowGridLines property. This property is used mostly for simplification of visual debugging. When it is set to true grid lines are drawn to visualize location of grid lines. |

<span id="uid-fb7a0fa0f1"></span>

### ColumnProperty

Column property. This is an attached property.
Grid defines Column property, so that it can be set
on any element treated as a cell. Column property
specifies child's position with respect to columns.

```csharp
public Avalonia.AttachedProperty<System.Int32> ColumnProperty
```

<span id="uid-61e77f55e1"></span>

### ColumnSpacingProperty

Defines the [Avalonia.Controls.Grid.ColumnSpacing](xref:Avalonia.Controls.Grid.ColumnSpacing) property.

```csharp
public Avalonia.StyledProperty<System.Double> ColumnSpacingProperty
```

<span id="uid-1f56a4de32"></span>

### ColumnSpanProperty

ColumnSpan property. This is an attached property.
Grid defines ColumnSpan, so that it can be set
on any element treated as a cell. ColumnSpan property
specifies child's width with respect to columns.
Example, ColumnSpan == 2 means that child will span across two columns.

```csharp
public Avalonia.AttachedProperty<System.Int32> ColumnSpanProperty
```

<span id="uid-f747297da8"></span>

### IsSharedSizeScopeProperty

IsSharedSizeScope property marks scoping element for shared size.

```csharp
public Avalonia.AttachedProperty<System.Boolean> IsSharedSizeScopeProperty
```

<span id="uid-439d1f6eff"></span>

### RowProperty

Row property. This is an attached property.
Grid defines Row, so that it can be set
on any element treated as a cell. Row property
specifies child's position with respect to rows.

Rows are 0 - based. In order to appear in first row, element
should have Row property set to `0`.

Default value for the property is `0`.

```csharp
public Avalonia.AttachedProperty<System.Int32> RowProperty
```

<span id="uid-ccd184b399"></span>

### RowSpacingProperty

Defines the [Avalonia.Controls.Grid.RowSpacing](xref:Avalonia.Controls.Grid.RowSpacing) property.

```csharp
public Avalonia.StyledProperty<System.Double> RowSpacingProperty
```

<span id="uid-ec23e31fee"></span>

### RowSpanProperty

RowSpan property. This is an attached property.
Grid defines RowSpan, so that it can be set
on any element treated as a cell. RowSpan property
specifies child's height with respect to row grid lines.
Example, RowSpan == 3 means that child will span across three rows.

```csharp
public Avalonia.AttachedProperty<System.Int32> RowSpanProperty
```

<span id="uid-17fdb6b70c"></span>

### ShowGridLinesProperty

ShowGridLines property. This property is used mostly
for simplification of visual debugging. When it is set
to `true` grid lines are drawn to visualize location
of grid lines.

```csharp
public Avalonia.StyledProperty<System.Boolean> ShowGridLinesProperty
```

</ApiRefPage>

