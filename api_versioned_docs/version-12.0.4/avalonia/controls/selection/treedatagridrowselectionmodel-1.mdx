---
title: 'TreeDataGridRowSelectionModel<TModel>'
hide_title: true
slug: '/avalonia/controls/selection/treedatagridrowselectionmodel-1'
uid: 'Avalonia.Controls.Selection.TreeDataGridRowSelectionModel`1'
commentId: 'T:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel`1'
docset: 'avalonia-docs'
packageId: 'Avalonia.Controls.TreeDataGrid'
assembly: 'Avalonia.Controls.TreeDataGrid'
apiKind: 'Class'
generatedBy: 'dotnet-apiref'
namespace: 'Avalonia.Controls.Selection'
tfms:
  - 'net8.0'
---

import ApiRefPage from '@site/src/components/apiref/ApiRefPage';

<ApiRefPage>

<h1 className="apiref-page__title">TreeDataGridRowSelectionModel&lt;TModel&gt; Class</h1>

## Definition

<div className="apiref-page__definition-meta">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Namespace:</span> <a href="./">Avalonia.Controls.Selection</a>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Assembly:</span> <code>Avalonia.Controls.TreeDataGrid</code>
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Package:</span> <code>Avalonia.Controls.TreeDataGrid</code>
</div>
</div>

<div className="apiref-page__lead">

Implements row selection functionality for a [Avalonia.Controls.TreeDataGrid](xref:Avalonia.Controls.TreeDataGrid) control.

</div>

<div className="apiref-page__signature">

```csharp
public class TreeDataGridRowSelectionModel<TModel>
```

</div>

<div className="apiref-page__relations">
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Inheritance:</span> object <span className="apiref-page__relation-separator">-&gt;</span> <a href="treedatagridselectionmodel">TreeDataGridSelectionModel</a> <span className="apiref-page__relation-separator">-&gt;</span> TreeDataGridRowSelectionModel&lt;TModel&gt;
</div>
<div className="apiref-page__meta-row">
<span className="apiref-page__meta-label">Implements:</span> ag, ah
</div>
</div>

## Remarks

TreeDataGridRowSelectionModel manages row selection in a [TreeDataGrid](xref:Avalonia.Controls.TreeDataGrid), supporting both
single and multiple selection modes. It tracks selection using [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)
instances that represent positions in the hierarchical structure.

Row selection supports the following user interactions:

- Clicking on rows to select them
- Using arrow keys to navigate between rows
- Using Shift key to select ranges of rows
- Expanding and collapsing hierarchical rows with arrow keys
- Type-to-select functionality for quickly finding rows

## Constructors

| Name | Description |
| --- | --- |
| [TreeDataGridRowSelectionModel&lt;TModel&gt;](#uid-0dc9e85b67) | No summary available. |

<section className="apiref-page__member-entry">

<span id="uid-0dc9e85b67"></span>

### TreeDataGridRowSelectionModel&lt;TModel&gt; Constructor

```csharp
public TreeDataGridRowSelectionModel<TModel>(Avalonia.Controls.TreeDataGridSource<TModel><TModel> source)
```

#### Parameters

`source` [Avalonia.Controls.TreeDataGridSource](xref:Avalonia.Controls.TreeDataGridSource)&lt;TModel&gt;&lt;TModel&gt;


</section>

## Methods

| Name | Description |
| --- | --- |
| [BatchUpdate](#uid-b429a7d5e5) | Creates a batch update operation that will defer selection change notifications until disposed. |
| [BeginBatchUpdate](#uid-ed5f30f702) | Begins a batch update of the selection. |
| [Clear](#uid-2f92e71c89) | Clears the selection. |
| [Deselect](#uid-faf9452a6f) | Deselects an item by its index in the source data. |
| [EndBatchUpdate](#uid-0bfd82da33) | Ends a batch update started by [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.BeginBatchUpdate](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.BeginBatchUpdate). |
| [IsSelected](#uid-8bfc2b06d0) | Queries whether an item is currently selected. |
| [Select](#uid-f5c53b35ba) | Selects an item by its index in the source data. |
| [SelectAll](#uid-3502c9e4bf) | Selects all currently visible rows. |
| [SelectRange](#uid-1d2e1f4810) | Selects an inclusive range of rows. |

<section className="apiref-page__member-entry">

<span id="uid-b429a7d5e5"></span>

### BatchUpdate Method

Creates a batch update operation that will defer selection change notifications until
disposed.

```csharp
public Avalonia.Controls.Selection.TreeDataGridRowSelectionModel<TModel>.BatchUpdateOperation<TModel><TModel> BatchUpdate()
```

#### Returns

Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;TModel&gt;.BatchUpdateOperation&lt;TModel&gt;&lt;TModel&gt;

A disposable object that, when disposed, will commit the selection changes and raise
appropriate events.


#### Remarks

Use this method with a using statement to group multiple selection operations together and
raise only a single set of change events when the batch is complete. This is equivalent to
calling [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.BeginBatchUpdate](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.BeginBatchUpdate) and [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.EndBatchUpdate](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.EndBatchUpdate).

</section>

<section className="apiref-page__member-entry">

<span id="uid-ed5f30f702"></span>

### BeginBatchUpdate Method

Begins a batch update of the selection.

```csharp
public void BeginBatchUpdate()
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-2f92e71c89"></span>

### Clear Method

Clears the selection.

```csharp
public void Clear()
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-faf9452a6f"></span>

### Deselect Method

Deselects an item by its index in the source data.

```csharp
public void Deselect(Avalonia.Controls.IndexPath index)
```

#### Parameters

`index` [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)

The index into the source data.


</section>

<section className="apiref-page__member-entry">

<span id="uid-0bfd82da33"></span>

### EndBatchUpdate Method

Ends a batch update started by [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.BeginBatchUpdate](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.BeginBatchUpdate).

```csharp
public void EndBatchUpdate()
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-8bfc2b06d0"></span>

### IsSelected Method

Queries whether an item is currently selected.

```csharp
public bool IsSelected(Avalonia.Controls.IndexPath index)
```

#### Parameters

`index` [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)

The index of the item in the source data.


#### Returns

bool

True if the item is selected; otherwise false.


</section>

<section className="apiref-page__member-entry">

<span id="uid-f5c53b35ba"></span>

### Select Method

Selects an item by its index in the source data.

```csharp
public void Select(Avalonia.Controls.IndexPath index)
```

#### Parameters

`index` [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)

The index path of the item to select.


</section>

<section className="apiref-page__member-entry">

<span id="uid-3502c9e4bf"></span>

### SelectAll Method

Selects all currently visible rows.

```csharp
public void SelectAll()
```

#### Remarks

Only rows that are currently materialized in the source (i.e. visible, taking into
account the expanded/collapsed state of parent rows) are selected; descendants of
collapsed rows are not selected. This method does nothing when
[Avalonia.Controls.Selection.TreeDataGridSelectionModel.SingleSelect](xref:Avalonia.Controls.Selection.TreeDataGridSelectionModel.SingleSelect) is true.

</section>

<section className="apiref-page__member-entry">

<span id="uid-1d2e1f4810"></span>

### SelectRange Method

Selects an inclusive range of rows.

```csharp
public void SelectRange(Avalonia.Controls.IndexPath start, Avalonia.Controls.IndexPath end)
```

#### Parameters

`start` [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)

The index of the row at which the range starts.


`end` [Avalonia.Controls.IndexPath](xref:Avalonia.Controls.IndexPath)

The index of the row at which the range ends.


#### Remarks

The range is selected by iterating the currently visible rows between
`start` and `end`; any existing selection is
cleared first. This method does nothing when
[Avalonia.Controls.Selection.TreeDataGridSelectionModel.SingleSelect](xref:Avalonia.Controls.Selection.TreeDataGridSelectionModel.SingleSelect) is true.

</section>

## Properties

| Name | Description |
| --- | --- |
| [AnchorIndex](#uid-53c460df07) | Gets or sets the anchor index. |
| [Count](#uid-8b55b5da6d) | No summary available. |
| [IsCellSelectionModel](#uid-6a10385dfa) | Gets a value indicating whether this is a cell selection model. |
| [RangeAnchorIndex](#uid-d7d024ac17) | Gets or sets the range anchor index. |
| [SelectedIndex](#uid-cdc1462ee3) | Gets or sets the selected index within the data. |
| [SelectedIndexes](#uid-5951beb0b0) | Gets the indexes of the selected items. |
| [SelectedItem](#uid-d03873e1f9) | Gets the currently selected item. |
| [SelectedItems](#uid-51a5504eb2) | Gets a collection containing all selected items. |
| [SingleSelect](#uid-c98ed464f3) | Gets or sets a value indicating whether only a single item can be selected at a time. |
| [Source](#uid-4053d3eb2c) | Gets the data source for the selection model. |

<section className="apiref-page__member-entry">

<span id="uid-53c460df07"></span>

### AnchorIndex Property

Gets or sets the anchor index.

```csharp
public Avalonia.Controls.IndexPath AnchorIndex { get; set; }
```

#### Remarks

The anchor index is the moving end of the selection: the item that keyboard
navigation acts on. Pressing an arrow key navigates relative to this index, and a
non-range selection updates it to the newly selected item. It is distinct from
[Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.RangeAnchorIndex](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.RangeAnchorIndex), which is the fixed pivot that a range extends from.

For example, after selecting the range 1 to 3 (by clicking item 1 then Shift-clicking
item 3, or by calling [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.SelectRange(Avalonia.Controls.IndexPath,Avalonia.Controls.IndexPath)](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.SelectRange%28Avalonia.Controls.IndexPath%2CAvalonia.Controls.IndexPath%29)), [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.RangeAnchorIndex](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.RangeAnchorIndex) is 1
(the pivot) while [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.AnchorIndex](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.AnchorIndex) is 3 (the moving end). A subsequent
Shift+Up then recomputes the range from the pivot 1 to the new end 2.

</section>

<section className="apiref-page__member-entry">

<span id="uid-8b55b5da6d"></span>

### Count Property

```csharp
public int Count { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-6a10385dfa"></span>

### IsCellSelectionModel Property

Gets a value indicating whether this is a cell selection model.

```csharp
public bool IsCellSelectionModel { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-d7d024ac17"></span>

### RangeAnchorIndex Property

Gets or sets the range anchor index.

```csharp
public Avalonia.Controls.IndexPath RangeAnchorIndex { get; set; }
```

#### Remarks

The range anchor index is the fixed pivot from which a range selection extends. It is
set when a fresh selection begins (for example when [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.SelectedIndex](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.SelectedIndex) is set,
or to the `anchor` argument of [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.SelectRange(Avalonia.Controls.IndexPath,Avalonia.Controls.IndexPath)](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.SelectRange%28Avalonia.Controls.IndexPath%2CAvalonia.Controls.IndexPath%29)) and is preserved while
the range is extended, so that repeated Shift-click or Shift+arrow operations all
extend from the same point.

Unlike [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.AnchorIndex](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.AnchorIndex), it is not moved by a plain [Avalonia.Controls.Selection.TreeDataGridRowSelectionModel&lt;T&gt;.Select(Avalonia.Controls.IndexPath)](xref:Avalonia.Controls.Selection.TreeDataGridRowSelectionModel%601.Select%28Avalonia.Controls.IndexPath%29)
call or by extending a range; only by operations that start a new selection.

</section>

<section className="apiref-page__member-entry">

<span id="uid-cdc1462ee3"></span>

### SelectedIndex Property

Gets or sets the selected index within the data.

```csharp
public Avalonia.Controls.IndexPath SelectedIndex { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-5951beb0b0"></span>

### SelectedIndexes Property

Gets the indexes of the selected items.

```csharp
public System.Collections.Generic.IReadOnlyList<Avalonia.Controls.IndexPath> SelectedIndexes { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-d03873e1f9"></span>

### SelectedItem Property

Gets the currently selected item.

```csharp
public TModel SelectedItem { get; set; }
```

#### Value

The selected item, or default(TModel) if no item is selected.


#### Remarks

When multiple items are selected, this property returns the first selected item.

</section>

<section className="apiref-page__member-entry">

<span id="uid-51a5504eb2"></span>

### SelectedItems Property

Gets a collection containing all selected items.

```csharp
public System.Collections.Generic.IReadOnlyList<TModel> SelectedItems { get; set; }
```

#### Value

A read-only list of the selected items.


</section>

<section className="apiref-page__member-entry">

<span id="uid-c98ed464f3"></span>

### SingleSelect Property

Gets or sets a value indicating whether only a single item can be selected at a time.

```csharp
public bool SingleSelect { get; set; }
```

</section>

<section className="apiref-page__member-entry">

<span id="uid-4053d3eb2c"></span>

### Source Property

Gets the data source for the selection model.

```csharp
public System.Collections.IEnumerable Source { get; set; }
```

</section>

## Events

| Name | Description |
| --- | --- |
| [SelectionChanged](#uid-20d38a3933) | Occurs when the selection changes. |
| [IndexesChanged](treedatagridselectionmodel.mdx#uid-c5c2106a08) | Occurs when item indexes change due to insertions or removals in the data source. Inherited from [TreeDataGridSelectionModel](treedatagridselectionmodel.mdx). |
| [PropertyChanged](treedatagridselectionmodel.mdx#uid-c2ee82eef9) | Inherited from [TreeDataGridSelectionModel](treedatagridselectionmodel.mdx). |
| [SourceReset](treedatagridselectionmodel.mdx#uid-eac05c7ba6) | Occurs when the data source is reset. Inherited from [TreeDataGridSelectionModel](treedatagridselectionmodel.mdx). |

<section className="apiref-page__member-entry">

<span id="uid-20d38a3933"></span>

### SelectionChanged Event

Occurs when the selection changes.

```csharp
public event EventHandler<Avalonia.Controls.Selection.TreeDataGridSelectionChangedEventArgs<T><TModel>> SelectionChanged
```

#### Remarks

This event provides strongly-typed access to the selection changes, including the
items and index paths that were selected and deselected.

</section>

</ApiRefPage>

